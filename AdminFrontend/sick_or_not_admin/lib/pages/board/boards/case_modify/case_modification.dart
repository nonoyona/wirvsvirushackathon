
import 'package:flutter/material.dart';

class CaseModification extends StatelessWidget {
  const CaseModification({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}